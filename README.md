# AI_Resume_Screener
An AI-powered web app built with Flask, spaCy, and PyPDF2 that analyzes and ranks multiple resumes based on a given job description. It extracts text from PDF resumes, computes semantic similarity using NLP, and displays ranked results as “Selected” or “Rejected” candidates.
